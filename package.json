{
  "name": "amaizo",
  "version": "1.0.0",
  "engines": {
    "node": "11.x"
  },
  "description": "",
  "main": "src/server/index.js",
  "scripts": {
    "install": "npm run install-client && npm run install-server",
    "install-client": "cd src/client && npm i",
    "install-server": "cd src/server && npm i",
    "dev": "concurrently \"cd src/server && npm i && npm start\" \"cd src/client && npm i && npm start\"",
    "heroku-postbuild": "concurrently \"cd src/server && npm start\" \"cd src/client && npm start\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "4.1.0",
    "webpack-cli": "3.3.1"
  }
}
