{
  "name": "amaizo",
  "version": "1.0.0",
  "engines": {
    "node": "11.x"
  },
  "description": "",
  "main": "src/server/index.js",
  "scripts": {
    "start": "webpack --mode production && node src/server/index.js",
    "test-run": "concurrently \"cd src/server && npm start\" \"cd src/client && npm i && npm start\"",
    "build": "webpack --mode production",
    "heroku-postbuild": "webpack --mode production"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "4.1.0"
  }
}
