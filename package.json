{
  "name": "amaizo",
  "version": "1.0.0",
  "engines": {
    "node": "11.x"
  },
  "description": "",
  "main": "src/server/index.js",
  "scripts": {
    "start": "concurrently \"cd src/server && npm i && npm start\" \"cd src/client && npm i && npm start\"",
    "heroku-postbuild": "concurrently \"cd src/server && npm i && npm start\" \"cd src/client && npm i && npm start\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "4.1.0"
  }
}
